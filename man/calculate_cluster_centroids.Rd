% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/architecture.R
\name{calculate_cluster_centroids}
\alias{calculate_cluster_centroids}
\title{Calculate Cluster Centroids and Assign Cluster IDs}
\usage{
calculate_cluster_centroids(data, cluster_labels)
}
\arguments{
\item{data}{A data frame or matrix with point coordinates, typically including columns for x, y, and z.}

\item{cluster_labels}{A vector of cluster labels (integers or factors) corresponding to each row in `data`.}
}
\value{
A data frame including the original point coordinates, a column `ID_Cluster` with cluster assignments,
and two additional columns `Centroid_X` and `Centroid_Y` representing the centroid coordinates of each cluster.
}
\description{
This function computes the centroid coordinates (mean of x, y, and z) for each cluster in a dataset
and assigns cluster IDs to the individual points. It returns a data frame containing the original points,
their assigned cluster IDs, and the corresponding cluster centroids.
}
\examples{
\dontrun{
 pts <- data.frame(x = runif(10), y = runif(10), z = runif(10))
 labels <- c(1,1,2,2,1,3,3,2,1,3)
 result <- calculate_cluster_centroids(pts, labels)
}
}
