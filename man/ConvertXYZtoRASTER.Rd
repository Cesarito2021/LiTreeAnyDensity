% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/architecture.R
\name{ConvertXYZtoRASTER}
\alias{ConvertXYZtoRASTER}
\title{Convert Point Cloud XYZ Data to Raster}
\usage{
ConvertXYZtoRASTER(data, feature, resolution, crs)
}
\arguments{
\item{data}{A data frame containing point cloud data with columns named "x", "y", and "z".}

\item{feature}{A character string specifying the feature to compute. Options are \code{"mean"} for mean z-values or \code{"count"} for point density.}

\item{resolution}{A numeric value specifying the resolution (pixel size) of the output raster.}

\item{crs}{A valid coordinate reference system (CRS) object or string (e.g., EPSG code) to assign to the output raster.}
}
\value{
A \code{RasterLayer} object representing the spatial distribution of the selected feature.
}
\description{
This function converts point cloud data (x, y, z) into a raster layer based on a specified feature, such as the mean elevation or point count per cell.
}
\examples{
library(raster)
df <- data.frame(x = runif(100), y = runif(100), z = rnorm(100))
r <- ConvertXYZtoRASTER(df, feature = "mean", resolution = 1, crs = "+init=epsg:32632")
}
