% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/architecture.R
\name{Extent_validation}
\alias{Extent_validation}
\title{Align Raster Extent and Resolution to a Reference Raster}
\usage{
Extent_validation(raster_ref, raster_to_correct)
}
\arguments{
\item{raster_ref}{A \code{RasterLayer} object used as the reference for extent, resolution, and CRS.}

\item{raster_to_correct}{A \code{RasterLayer} object that will be resampled and aligned to match the reference raster.}
}
\value{
A \code{RasterLayer} object with extent, resolution, and CRS aligned to \code{raster_ref}.
}
\description{
This function adjusts the extent, resolution, and coordinate reference system (CRS) of a raster object to match a reference raster.
}
\examples{
library(raster)
r1 <- raster(ncol=10, nrow=10)
extent(r1) <- c(0, 10, 0, 10)
crs(r1) <- CRS("+init=epsg:4326")
r2 <- raster(ncol=5, nrow=5)
extent(r2) <- c(1, 6, 1, 6)
crs(r2) <- CRS("+init=epsg:3857")
r_corrected <- Extent_validation(r1, r2)
}
